function addBinary(a, b) {
    return (parseInt(a - 0, 2) + parseInt(b - 0, 2)).toString(2)
}
console.log(addBinary('11', '1')); // 100

/**
 * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
 *
 * 
 * @param nums int整型一维数组 number[]
 * @return string字符串
 */
 function largestNumber( nums ) {
    // write code here
    let str = ''
    
}